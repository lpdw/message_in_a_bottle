{% extends 'base.html.twig' %}

{% block content%}
    <h1>Forest</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forest.id }}</td>
            </tr>
        </tbody>
    </table>
    <div style="height:500;width:100%;background-color:lightgrey;">
        {% block script %}


        {% endblock %}
    </div>

    <div>
        <script>
        jQuery(document).ready(function(){
            $.ajax({
            dataType: "json",
            url: "../../web/json/JDVELH.json",
            success: 'bravo'
            });
            console.log(data);
        });
        </script>
    </div>

    <ul>
        <li>
            <a href="{{ path('forest_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('forest_edit', { 'id': forest.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
