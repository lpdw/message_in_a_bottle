{% extends 'base.html.twig' %}

{% block content%}
    <h1>Île n°{{island.id}}</h1>

    <ul>
        <li>
            Coordonnées :
            {{ island.localisationX }} ; {{ island.localisationY }}
        </li>
        <li>
            Type :
            {{ island.type|capitalize }}
        </li>
    </ul>

    <ul>
        <li>
            <a href="{{ path('island_index') }}">Retour à la Map</a>
        </li>
    </ul>
{% endblock %}
