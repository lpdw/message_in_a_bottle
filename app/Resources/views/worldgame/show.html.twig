{% extends 'base.html.twig' %}

{% block content%}
    <h1>Worldgame {{ worldGame.id }}</h1>

    <!-- <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ worldGame.id }}</td>
            </tr>
            <tr>
                <th>Grid</th>
                <td>{% if worldGame.grid %}{{ dump(worldGame.grid) }}{% endif %}</td>
            </tr>
        </tbody>
    </table> -->

    World Map :<br>
    <table id="worldmap">
        <tbody>
            {% for islands in worldGame.grid %}
                <tr>
                    {% for island in islands %}
                        <td>
                            {% if island is null %}
                                ~
                            {% elseif island == 'coast' %}
                                ^
                            {% else %}
                                {% if island.id is null %}
                                    ?
                                {% else %}
                                    <b><a href="{{ path('island_show', { 'id': island.id }) }}">{{island.id}}</a></b>
                                {% endif %}
                            {% endif %}
                        </td>
                    {% endfor %}
                </tr>
            {% endfor %}
    </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('worldgame_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('worldgame_edit', { 'id': worldGame.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
