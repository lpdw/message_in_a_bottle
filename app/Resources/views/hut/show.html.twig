{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hut</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hut.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('hut_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('hut_edit', { 'id': hut.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
